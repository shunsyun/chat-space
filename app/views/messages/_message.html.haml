.message
  .message__top-info
    %p.message__top-info__user-name
      = message.user.name
    %p.message__top-info__date
      = message.created_at.strftime("%Y/%m/%d %H:%M")
  %p.message__message-text 
    - if message.content.present?
      %p.message__message-text
        = message.content
    = image_tag message.image.url, class: 'lower-message__image' if message.image.present?
-# .message
-#   .message__top-info
-#     %p.message__top-info__user-name  name
-#     %p.message__top-info__date 2019-5/16
-#   %p.message__message-text message
-#   .message
-#   .upper-message
-#     .upper-message__user-name
-#       = message.user.name
-#     .upper-message__date
-#       = message.created_at.strftime("%Y/%m/%d %H:%M")
-#   .lower-meesage
-#     - if message.content.present?
-#       %p.lower-message__content
-#         = message.content
-#     = image_tag message.image.url, class: 'lower-message__image' if message.image.present?